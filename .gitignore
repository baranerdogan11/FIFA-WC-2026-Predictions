# data: everything regenerable is ignored (src/download_data.py + pipeline);
# the two browser-collected stats files are committed because no script can
# re-fetch them (FBref/Sofascore block non-browser access)
data/*
!data/raw/
data/raw/*
!data/raw/sofascore_match_stats.csv
!data/raw/fbref_squad_stats.csv

models/

# python
__pycache__/
*.pyc
.ipynb_checkpoints/

# os / editor
.DS_Store
.vscode/
.idea/
